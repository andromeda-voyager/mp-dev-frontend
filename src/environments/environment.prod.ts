import { CHESSBOARD } from 'src/app/chess/shared/models/starting-chessboard';

export const environment = {
  production: true,
  BASE_API_URL: "https://api.matthewpotts.dev",
  STARTING_CHESSBOARD: CHESSBOARD
};